<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.android.cericatto.jobsity">

    <!--=============== PERMISSIONS ===============-->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--=============== APPLICATION ===============-->

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name="br.android.cericatto.jobsity.view.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity
            android:name="br.android.cericatto.jobsity.view.activity.ShowDetailsActivity"
            android:parentActivityName="br.android.cericatto.jobsity.view.activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="br.android.cericatto.jobsity.view.activity.MainActivity" />
        </activity>

        <activity
            android:name="br.android.cericatto.jobsity.view.activity.ShowEpisodesActivity"
            android:parentActivityName="br.android.cericatto.jobsity.view.activity.ShowDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="br.android.cericatto.jobsity.view.activity.ShowDetailsActivity" />
        </activity>
    </application>
</manifest>